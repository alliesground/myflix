%section.register.container
  .row
    .col-sm-10.col-sm-offset-1
      = bootstrap_form_for @user, layout: :horizontal, label_col: "col-sm-2", control_col: "col-sm-10" do |f|
        = f.alert_message "Please fix the errors below.", error_summary: false
        %header
          %h1 Register
        %fieldset
          = hidden_field_tag :invitation_token, @invitation.token if @invitation
          
          = f.email_field :email, label: "Email Address"
          = f.password_field :password
          = f.text_field :full_name, label: "Full Name"
          = f.form_group do
            %script(src="https://checkout.stripe.com/checkout.js" class="stripe-button" data-key="#{ENV['stripe_publishable_key']}" data-amount="999" data-name="Sign up charge" data-description="9.99 to sign up for videos")